//- To specify class names on elements use . and add name of class
header.header
  nav.nav.nav--tours
    a.nav__el(href="/") All tours

  //- Don't need to specify div element can just add class and will auto render as a div element
  .header__logo
    img(src="/img/logo-white.png" alt="Natours logo")

  nav.nav.nav--user
    //- Use pug conditionals to dynamically render HTML based on validation (can only use basic conditionals with pug)
    //- Template gets access to user variable via authController setting local key on response and adding a value
    if user
      a.nav__el.nav__el--logout Log out
      a.nav__el(href="/me")
        img.nav__user-img(src=`/img/users/${user.photo}` alt=`Photo of ${user.name}`)
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Log in
      a.nav__el.nav__el--cta(href="#") Sign up
